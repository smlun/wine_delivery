.row
  = link_to "", wines_path, class: 'glyphicon glyphicon-chevron-left'

.row
  .col-xs-5
    img src="http://placehold.it/400x550"
  .col-xs-7
    h1 = @wine.name
    p = @wine.year
    p = @wine.description
    p.pull-right = @wine.price

.row
  = link_to "DELETE", wine_path(@wine), method: :delete, data: { confirm: 'ARE YOU SURE?' }